namespace FiveStack.Enums;

// Mirrors public.e_utility_throw_strengths. CS2 has exactly three release
// strengths: left click, both buttons, right click.
public enum eThrowStrength
{
    Full,
    Half,
    Drop,
}
