namespace PlayCs;

public partial class PlayCsPlugin { }

//
//
// if (player.member && player.side !== player.member.team.starting_side) {
//     logger.info(
//         `[${matchId}] move ${steamId}:${player.name}`,
//     player.member.team.starting_side,
//         );
//     await this.command(
//         matchId,
//         `move_player "${steamId}" "${player.member.team.starting_side}"`,
//         );
// }
